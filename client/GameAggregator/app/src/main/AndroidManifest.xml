<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gameaggregator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameAggregator">
        <activity
            android:name=".activities.GameDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ChooseTypeActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ChooseAgeActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ChooseEventActivity"
            android:exported="false" />
        <activity
            android:name=".activities.FilteredGamesActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>